gsap.from(".hero-img", {
    y: "25rem",
    rotation: 60, // Correct property for rotation
    duration: 2,   // Add a duration (in seconds) for the animation
    ease: "power3.out" // Optional easing for a smooth effect
});
